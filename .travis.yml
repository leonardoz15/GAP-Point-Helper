language: python
python:
  - "3.6"
  
#Markdown linter for README.md  
before_install:
  - gem install mdl
  
install:
  pip install -r pygsheets

script:
    - mdl README.md
    
before_script: cd src

script:
  - python main.py
  
after_success:
  coveralls
